language: rust

rust:
  1.25.0

cache: cargo

before_script:
- rustup component add rustfmt-preview

script:
- cargo fmt --all -- --check
- cd toybox && cargo test --verbose --all
- cd openai && cargo build --verbose --all
